# Architecture

## Component Overview

SOVA has four main components:

### 1. CLI (`sova/cli/`)
- Python CLI built with Typer, entry point `sova` (via pyproject.toml)
- Subcommands: `run`, `watch`, `parallel`, `triage`, `harden`, `install`, `setup`, `uninstall`, `dashboard`, `server`, `commands`, `memory`, `status`, `costs`, `cleanup`, `doctor`, `address-pr`, `maintain-pr`, `review-pr`, `learn-from-pr`, `init-db`, `config`, `mcp`, `supervisor`
- Registered in `sova/cli/app.py`, implementations in `sova/cli/commands/`

### 2. Agent Core (`sova/core/`, `sova/roles/`)
- `core/workflow.py` -- WorkflowEngine: executes step pipelines with DB persistence (TaskRun, StepExecution, FailureRecord)
- `core/state.py` -- 19-state TaskStatus StrEnum with transition validation
- `core/context.py` -- ExecutionContext dataclass threading state through steps
- `core/output.py` -- OutputWriter for per-run DB-backed output persistence, read_lines, retention cleanup
- `core/steps/` -- 29 BaseStep implementations with execute/validate_output/can_skip. Four pipeline variants:
  - **Developer pipeline** (16 steps): sync -> assess -> create_worktree -> capture_baseline -> develop -> simplify -> self_review -> commit -> validate -> push -> create_pr -> wait_for_external_reviews -> address_external_findings -> monitor_ci -> extract_memory -> handoff_to_reviewer
  - **Address-review pipeline** (10 steps): ensure_worktree -> rebase -> address_review -> rearrange_commits -> validate -> push -> monitor_ci -> resolve_external_reviews -> extract_memory -> handoff_to_user
  - **Researcher pipeline** (4 steps): fetch_task -> research -> spec -> extract_memory
  - **Planner pipeline** (4 steps): scan_project -> generate_tasks -> validate_tasks -> extract_memory
- `core/dag.py` -- DAGExecutor: runs command-based workflow graphs with topological sort, condition evaluation, and cycle detection
- `roles/` -- AgentRole ABC with 6 implementations: triage, researcher, developer, reviewer, custom, planner
- `roles/custom.py` -- CustomRole: executes user-defined DAG workflows via DAGExecutor
- `roles/dispatcher.py` -- routes tasks to appropriate roles based on state; `get_role_async()` falls back to DB lookup for custom roles
- **Role chaining**: Developer -> Reviewer -> Developer handoff chain runs autonomously by default. `HandoffAction.auto_execute` triggers auto-spawn of the next agent when the current one exits. Developer writes handoff to Reviewer, Reviewer writes handoff back to Developer if findings exist, or to user if clean (manual "Integrate PR" button). Both auto-spawn directions are configurable in `sova.toml`: `pipeline.auto_handoff` (Developer->Reviewer, default true) and `pipeline.auto_address_review` (Reviewer->Developer, default true). When disabled, handoff files are still written with `auto_execute=False` so the dashboard shows manual action buttons instead. Issue stays `IN_REVIEW` until human merges via `/integrate-pr` or `/approve-merge`.
- **Address-review circuit breaker**: prevents infinite bot re-review loops (e.g., CodeRabbit repeatedly requesting changes after each address-review cycle). `_count_address_review_runs()` in `agent_handoff.py` counts completed address-review runs by querying `TaskRun` records that have a `StepExecution` with `step_name="address_review"`, filtered by issue number and PR number. When the count >= `pipeline.max_address_review_cycles` (default 2, 0=unlimited), `_process_auto_handoff()` blocks auto-execution and writes a manual-only handoff so the dashboard shows "Address Review (manual)" and "Integrate PR" buttons instead of auto-spawning another cycle.
- **Step gate checks**: every `validate_output()` must check all forms of change -- unstaged diff (`git diff --stat HEAD`), staged diff (`git diff --cached --stat`), commits ahead of base (`git log {base}..HEAD --oneline`), and untracked new files (`git status --porcelain`, lines starting with `??`). LLM agents may commit directly, leaving working-tree diffs empty even when real work was done; they may also write entirely new files without staging them, which `git diff` and `git log` both miss. Never return `GateCheckResult(passed=True)` unconditionally. `DevelopStep.validate_output()` additionally collects filenames from all change sources (unstaged, staged, committed, untracked) and verifies that at least one is a substantive source file (not just lockfiles or metadata like `.sova/`, `.claude/`) via `_NON_SUBSTANTIVE_RE`. `DevelopStep.execute()` returns `success=False` when the inner check loop fails: `_run_inner_check_loop()` returns a `(passed: bool, summary: str)` tuple, and any non-passing outcome (exhausted cycles, budget exceeded, no changes produced, test weakening, LLM fix failure) sets `passed=False`.
- **Context persistence at step boundaries**: `_sync_task_run_context()` persists `worktree_path`, `branch_name`, and `pr_number` to the TaskRun after every step. This ensures the checkpoint/resume system can restore context even if the run pauses or crashes mid-pipeline. Dashboard's `_create_task_run()` also accepts `pr_number` so reviewer runs spawned via auto-handoff have it recorded immediately (without relying on WorkflowEngine sync).

### 3. Dashboard (`sova/dashboard/`)
- Python/FastAPI web UI with app factory pattern (`create_app(project_dir=None)`)
- Jinja2 templates + Tailwind CSS (prebuilt via `make css`), Catppuccin dark theme
- 25 pages: dashboard, agents, run_detail, lifecycle, costs, pr_metrics, queue, specs, logs, settings, memory, setup, home, style_guide, roles, role_editor, spec, control, overview, runs, tasks, base, supervisor, fleet, oversight
- **Design system**: CSS variables (Catppuccin Mocha) in `static/style.css`, Tailwind config in `tailwind.config.js` (repo root), SVG icon macro in `_icons.html`, component macros in `_components.html`. Run `make css` after adding or removing Tailwind classes in templates/JS (the prebuilt `static/tailwind.min.css` is checked in, not built by CI)
- 26 API routers under `/api`: overview, runs, costs, control, feed, handoff, lifecycle, memory, logs, tasks, queue, quota, settings, setup, agents, work, roles, spec, prs, dependencies, resources, supervisor, oversight, fleet_manager, fleet_insights, telemetry
- 38 services: run, cost, memory, control (facade), feed, handoff, lifecycle, queue, batch, work, work_item, task, log, settings, setup, agent_lifecycle, agent_output, agent_recovery, agent_handoff, agent_pool, agent_db, agent_status, agent_context, agent_progress, agent_validation, output (re-export facade for core/output), role, spec, pr, pr_metrics, resource, llm_suggestion, output_stream, fleet, fleet_manager, supervisor, oversight, telemetry_push
- Old pages (overview, control, runs, tasks, work) redirect to current equivalents (dashboard or agents)
- **Multi-agent control**: manages concurrent agent processes per project with slot limits and per-issue dedup. Both `start_agent()` and `start_command()` call `_check_issue_conflict()` which rejects duplicates via two checks: in-memory (`pa.agents`) and DB (`TaskRun` with alive PID). The DB check catches CLI-spawned agents not tracked in-memory. The `max_concurrent` slot check alone doesn't prevent same-issue duplicates. `_check_issue_conflict()` auto-recovers dead-PID DB runs by marking them "interrupted" on detection. When `force=True` (passed through from `start_agent`), both in-memory and live external conflicts are skipped so `--force` retries are not blocked by stale state.
- **Batch operations**: triage/harden multiple issues with parallel concurrency (`asyncio.Semaphore`, default 3 for triage, 2 for harden via `DEFAULT_CONCURRENCY`). `BatchJob.max_concurrency` configurable per-batch. Global progress bar in `base.html` (visible on all pages), batch ID persistence via `sessionStorage`, `GET /api/queue/batch/active` endpoint for discovering running batches after page navigation or browser refresh
- **Handoff system**: agents write `.claude/agent-control/handoff.json` to pass state between agents
  - `handoff_service.py` -- read/write/archive handoff files (mtime-cached)
  - Dashboard renders handoff action buttons on the agents page (awaiting_action/completed/failed). Failed runs also show a "Re-run" button that pre-fills issue, role, and PR number.
  - `_process_auto_handoff()` in `agent_handoff.py` auto-triggers `HandoffAction` entries with `auto_execute=True` after agent exit, enabling autonomous role chaining. The `auto_execute` flag is set per `pipeline.auto_handoff` / `pipeline.auto_address_review` config
  - Enables chaining: `integrate-pr` (full pipeline: rebase, CI, merge, cleanup)
- **Claude command execution**: `agent_lifecycle.start_command()` runs Claude Code commands from handoff actions (re-exported via `control_service` facade)
- Tests: `tests/test_dashboard.py` + `tests/test_batch_service.py` (pytest + httpx ASGITransport, in-memory SQLite via `sqlite+aiosqlite://`), run via `make test-py`

### 4. Scheduler (`sova/scheduler/`)
- `watch.py` -- WatchLoop: async poll with priority scan (RESEARCHED > TRIAGED > BACKLOG), veto window, asyncio.Event for shutdown
- `parallel.py` -- ParallelExecutor: asyncio.Semaphore for max_parallel_agents
- `server.py` -- SOVAServer: combined FastAPI dashboard + scheduler in one process, PID file lifecycle
- Deploy: `deploy/sova-server.service` (systemd) + `deploy/com.sova.server.plist` (launchd)
- CLI: `sova server start/stop/status`

## Supporting Modules

- **`sova/adapters/`** -- TaskAdapter ABC + GitHub and Jira Cloud implementations (state via `agent:` labels), factory, per-project auth via `sova/utils/gh.py` (GitHub) and httpx (Jira)
- **`sova/llm/`** -- Provider-agnostic LLM interface. `provider.py` defines the `LLMProvider` ABC (invoke, invoke_streaming, invoke_batch, invoke_command, normalize_model_name, check_available) and `create_provider` factory. `invoke_batch()` is a concrete default (sequential fallback via `invoke()` N times) so only batch-capable providers need to override. `providers/claude_code.py` is the default implementation wrapping the Claude Code CLI. `litellm_provider.py` adds LiteLLM multi-provider integration with automatic fallback. `providers/anthropic_batch.py` implements `BatchProvider` with dual backends: Vertex AI (GCS + batchPredictionJobs via httpx + google-auth ADC) and Anthropic direct (Message Batches API via httpx). Auto-detected by `create_batch_provider()` (Vertex when `ANTHROPIC_VERTEX_PROJECT_ID` + `batch_gcs_bucket` set, Anthropic when `ANTHROPIC_API_KEY` set, None otherwise). The batch provider is special-purpose (instantiated on-demand, never registered as global provider). `client.py` is a thin delegation layer with module-level `get_provider()`/`set_provider()` so existing callers (`from sova.llm import invoke`) work unchanged; `invoke_batch()` auto-detects the batch provider before falling back to the global provider's sequential default. `cost.py` records cost data. `models.py` defines `LLMResult`, `StreamEvent`, `BatchRequest`, `BatchResult`, and `BatchTimeoutError`. CLI output formats: `--output-format json` returns `{result, total_cost_usd, usage, duration_ms, session_id}`; `--output-format stream-json` emits JSONL (`type: "assistant"`, `type: "result"`). Dashboard's `_parse_stream_line()` extracts readable text from stream events.
- **`sova/git/`** -- branch/commit/push (`branch.py`), PR operations (`pr.py`), LLM rebase (`rebase.py`), worktree lifecycle (`worktree.py`). `operations.py` is a thin re-export facade. Always wrap `json.loads(result.stdout)` in try/except when parsing `gh` CLI output -- even successful commands can return empty stdout in edge cases.
- **`sova/ipc/`** -- `_BaseAgentProcess` (shared process delegation), `AgentProcess` (pipe-based I/O) and `FileAgentProcess` (file-based I/O for dashboard restart survival), `AgentRuntime` ABC + `ClaudeCodeRuntime`/`AiderRuntime` (pluggable agent backends via `create_runtime()` factory, `output_dir`/`run_label` params for file-based output, global state via `get_runtime()`/`set_runtime()`), AgentHandoff + DashboardHandoff models, notifications (desktop + Slack). Dashboard-spawned agents use file-based output (`.claude/agent-output/{run_id}.stdout/.stderr`); CLI-spawned agents use pipes.
- **`sova/knowledge/`** -- Memory CRUD + search + promote, tier loading, persona detection, review patterns, team sharing (export/import across installations)
- **`sova/commands/`** -- command and skill distribution: catalog (discover + classify + skills dir), templates (regex rendering), manifest (SHA-256 tracking), distribution (install/update/diff with conflict detection for commands, guidelines, and skills)
- **`sova/config/`** -- Pydantic Settings v2, TOML loader, project registry, per-request project context (`context.py`, moved from dashboard/project_context.py)
- **`sova/db/`** -- SQLAlchemy 2.0 async ORM models (TaskRun, OutputLine, StepExecution, FailureRecord, CostRecord, Memory, TaskAssessmentRecord, IssueLifecycle, LifecyclePhaseRecord, WorkflowDefinition, CommandContract, PREvent), session factory (SQLite default, PostgreSQL optional). See `docs/database-guidelines.md` for session patterns, JSON column gotchas, and migration conventions.
- **`sova/supervisor/`** -- Supervisor-level services. `progression.py`: `TaskProgressionEngine` evaluates all active tasks on each poll cycle and produces `ProgressionDecision` objects. Deterministic state machine with 13 gate checks (GitHub API rate limit, memory pressure, dependency graph, CodeRabbit quota, CI budget, agent slot capacity, per-issue budget, duplicate agent detection, repeated researcher failure, ownership, merge conflict, file overlap, address-review circuit breaker). GitHub API rate limit gate runs first (global, precomputed once per cycle via identity-keyed `github_quota.py` trackers); fails open if tracker unavailable. Rate limit check runs before `build_dependency_graph()` to avoid wasting API calls during cooldown. Memory pressure gate runs second (global system condition, precomputed once per cycle); fails open on psutil errors; warn/block thresholds configured via `[memory_guard]` in `sova.toml` (`enabled`, `warn_threshold_gb`, `block_threshold_gb`). Repeated failure gate (`_check_repeated_failures_gate`) blocks SPAWN_RESEARCHER when the count of failed researcher runs since the last successful run >= `supervisor.max_researcher_failures` (default 3, 0=unlimited); prevents infinite re-spawn loops on closed or mis-labelled issues. Config: `SupervisorConfig` in `sova.toml` under `[supervisor]` (enable flag, per-phase auto flags, `require_approval`, `respect_dependencies`, `poll_interval_seconds`, `max_researcher_failures`, `max_spawns_per_cycle`, `ci_warn_minutes`, `ci_block_minutes`). `require_approval=True` (default) stores actionable progression decisions in an in-memory plan instead of executing them; the supervisor dashboard shows a "Pending Actions" panel with per-item approve/skip controls. `auto_research` defaults to `False` (previously `True`) so new installs do not spawn researchers on all triaged issues on first start. `auto_address_review` enables supervisor-level address-review spawning when a SOVA review verdict is "revise" or "block" (complements the handoff-based intra-pipeline chaining with a supervisor fallback for command-path reviews). `github_quota.py`: identity-keyed in-memory rate limit state trackers for GitHub API; records hits from the shell layer per `github_user`, provides cooldown gate for the progression engine, PR monitor, and `api_health` status for the dashboard; emits feed events on hit/recovery transitions. `get_github_quota_tracker(identity)` returns a per-identity tracker; all callers pass `github_user` from config or adapter. `track_rate_limit(result, identity)` is the shared function called by `GitHubAdapter._track_rate_limit()`, `sova/git/pr.py:_track_gh_rate_limit()`, and `sova/supervisor/pr_monitor.py:_track_gh_rate_limit_pr_monitor()` to feed the tracker from all `gh` CLI call sites. `pr_monitor.py`: `PRMonitor._poll_cycle()` checks `should_skip()` both before API calls and after `asyncio.gather()` to catch rate limits triggered mid-cycle by `_is_coderabbit_rate_limited`. `coderabbit_quota.py`: quota tracking for CodeRabbit review rate limits. `ci_budget.py`: GitHub Actions CI minutes tracking via billing API with TTL caching; identity-keyed trackers matching `github_quota.py` pattern; fail-open on API errors (zero-state budget never blocks). `dependency_graph.py`: builds a `DependencyGraph` from issue bodies (parses `## Dependencies` sections). Issues labeled `type: epic` are tracking containers and are excluded from `get_ready_tasks()` (treated similarly to `HUMAN_ONLY` state). Epic dependencies are skipped by both `_are_dependencies_satisfied()` and `_check_dependency_gate()`: when a child issue lists an epic in its Dependencies section, the epic does not block the child (epics are tracking containers, not real blockers). Without this, a deadlock occurs: children blocked by the non-DONE epic, but the epic can only auto-close when all children are DONE. Epics are auto-closed by `TaskProgressionEngine.auto_close_epics()` when all child issues (those listing the epic in their Dependencies section) reach `DONE` state. `_STATE_ACTIONS` maps each `TaskState` to the actions surfaced in the graph drawer (e.g., BACKLOG exposes Triage, TRIAGED/NEEDS_SPEC expose Run Researcher, RESEARCHED exposes Run Developer, IN_REVIEW exposes Integrate PR and Address PR). `planner.py`: `SupervisorPlanner` assembles a resource snapshot (GitHub quota, CodeRabbit quota, CI budget, agent slots, open PRs, issue counts, recent failures) and supervisor persona (#600), calls the Anthropic API (Sonnet via direct httpx, matching `llm_suggestion_service.py` pattern) to produce a structured `PlanResult` with reasoning, approved actions, and deferred items. `evaluate_all(plan=...)` filters actionable decisions against the plan (can only subtract, never add/unblock). Gated behind `supervisor.llm_planning` config (default false); returns None on missing API key, timeout, or error (falls back to deterministic mode). Persona braces are escaped before `str.format()` to prevent `KeyError` on user-authored `{text}` in persona files.
- **Supervisor API caching layer**: four TTL caches reduce GitHub API consumption from ~4,800 calls/hr to ~1,000: (1) `coderabbit_quota.py:_sync_cache` (300s TTL, per-repo single-flight lock, `None` return on API failure prevents caching errors); (2) `pr_monitor.py:_rate_check_cache` (300s TTL, keyed by `(repo, pr_number)` tuple, halved TTL for rate-limited results); (3) `pr_service.py:_pr_cache` (60s TTL, shared across supervisor/monitor/dashboard); (4) `dependency_graph.py:_graph_cache` (120s TTL, per-repo, invalidated on agent spawn and epic close, milestone-filtered builds bypass). The daemon enforces a 60s minimum poll interval (`_MIN_POLL_INTERVAL`) via runtime clamping. Cache key resolution uses `adapter.repo` (GitHub) or `adapter.project_key` (Jira) for cross-adapter compatibility.
- **`.claude/benchmark/`** -- Development velocity benchmark infrastructure. `resolve_root.sh`: shared bash function mirroring `get_primary_worktree_root()` that resolves the main checkout path from any worktree via `git rev-parse --git-common-dir`, sourced by all other scripts so JSONL logs always write to the primary checkout (surviving worktree cleanup). `log.sh`: JSONL event logger for issue-level benchmarks (session_start, develop_complete, pr_created, etc.). `session_start_hook.sh`: Claude Code SessionStart hook that creates `issue-{N}.jsonl` and logs `session_start` when opening a session on a benchmark branch (`feat/issue-NNN`). `session_end_hook.sh`: Claude Code Stop hook that captures session metrics (model, duration, token counts, cost) from transcript files and appends to benchmark log; self-healing (creates the log file if `session_start` didn't run). Both hooks registered in `.claude/settings.json` (SessionStart + Stop); `_copy_claude_artifacts` copies `settings.json` to SOVA-created worktrees so hooks fire there. For full coverage in Claude Code-created worktrees (EnterWorktree), add the same hooks to `~/.claude/settings.json`. Logs stored at `.claude/benchmark/issue-{N}.jsonl`, gitignored. Used for SOVA vs interactive development velocity comparison (issue #6).

## Config System
- **SOVA config**: `sova.toml` per project (Pydantic Settings, env var overrides via `SOVA_` prefix)
- **DB URL**: `SOVA_DATABASE_URL` env var for PostgreSQL; defaults to `.claude/sova.db` (SQLite)
- **Budget limits**: per-run and per-issue caps prevent runaway costs. See `docs/performance-guidelines.md` for defaults and config keys.
- **Settings metadata registry**: every field in a config model (`models.py`) must also have a `SettingMeta` entry in `sova/dashboard/settings_meta.py` with key, label, description, group, and value_type. Without it, the field won't appear in the dashboard settings UI. When adding a new config group (e.g., `external_reviews`), also add it to `GROUPS` dict and `GROUP_ORDER` list.

## Naming Convention

The project's full name is **SOVA** (Software Orchestration Via Agents).

- **CLI command**: `sova`
- **PyPI package**: `sova`
- **Config files**: `sova.toml`, `sova.db`

## Development Workflow
- `Makefile` at repo root provides all development targets
- `make serve` -- start dashboard
- `make check` -- lint + test (CI-equivalent)
- `make test` -- bash (shellcheck + invariant --help) + python (pytest)
- `make lint` -- shellcheck + ruff
- `make format` -- ruff auto-format

## Key Design Decisions
- **Python for SOVA**: unified stack for CLI, agent, dashboard. Zero-config SQLite for single dev.
- **Role-based agents**: triage, researcher, developer, reviewer with dispatcher routing
- **Gate checks between steps**: every step validates output before the next starts
- **Ephemeral agents**: spawn, work, write handoff, die. No persistent sessions.
- **Worktree isolation**: each task gets its own git worktree (parallel-safe)
- **Adapter pattern for task sources**: swap GitHub/JIRA/Linear without touching core
- **Mandatory pipeline**: Triage -> Researcher -> Developer (enforced by Gate 3); `--force` bypasses
- **Split pipelines at role boundaries**: each role has its own step pipeline (developer: 16 steps, address-review: 10 steps, researcher: 4 steps, planner: 4 steps). A single monolithic pipeline that crosses role boundaries (develop + review + address in one sequence) breaks agent isolation and prevents independent lifecycles. The role detects which pipeline variant to run from `ctx.pr_number` alone (set by handoff). Issue state (`task.state`) is NOT used for variant detection because the dashboard may race it to `in_progress` before the role reads it.
- **Issue state ownership is human**: agents never auto-move issues to DONE. Issues stay IN_REVIEW until the human merges via `/integrate-pr` or `/approve-merge`. The agent prepares the PR; the human approves and merges.
- **Handoff protocol**: JSON-based inter-agent state passing via file + DB
- **Short-lived agent model**: agents run, write handoff, exit; dashboard provides the interactive bridge
- **Markdown commands**: Claude Code loads them as slash commands, 30 commands with category frontmatter
- **Persona auto-detection**: detects project tech stack and loads relevant guidance
- **DB persistence**: TaskRun, CostRecord, StepExecution tracked in SQLite/PostgreSQL
- **Unified TaskRun via `--run-id` passthrough**: the dashboard creates a TaskRun (with PID for process management), passes `--run-id {id}` to the subprocess, and WorkflowEngine adopts it via `_adopt_task_run()` instead of creating a second record. This eliminates the "wrapper agent" confusion on the Work page. `_adopt_task_run()` clears the `current_step="agent"` sentinel and preserves the dashboard's PID. The dashboard's `_finalize_task_run()` only updates status if not already terminal (the engine may finalize first), but always writes stream cost (more accurate). CLI runs without `--run-id` still create their own TaskRun.
- **Combined server**: `sova server start` runs dashboard + scheduler in one async process
- **Idempotent finalization**: when multiple codepaths can finalize state (e.g., WorkflowEngine completion + `_wait_and_finalize`, background sweep + manual mark-failed), guard with a status check before writing. `_finalize_task_run()` in `agent_db.py` uses `_TERMINAL_STATUSES = {"done", "failed", "rejected", "interrupted", "paused"}` -- if the run is already in one of these states, status is not overwritten but cost is still updated (stream cost is more accurate). Any endpoint that sets a run's status to terminal (e.g., `mark-failed`) must also call `stop_agent(run_id=...)` to kill the subprocess, otherwise `_wait_and_finalize()` blocks forever on `await process.wait()`.
- **Stale run recovery + dismiss**: `recover_stale_runs()` queries all non-terminal TaskRuns (`status.notin_(_TERMINAL)`) on dashboard startup and marks dead-PID ones as "interrupted" -- not just `status == "running"`, so runs stuck in "pending", "assessing", etc. are also caught. `POST /api/agents/interrupted/dismiss` changes interrupted runs to "failed" so users can clear the banner. The "interrupted" status must be in `_TERMINAL` sets in `work_service.py`.
- **Lifespan shutdown must cancel ALL background tasks**: `_shutdown_tasks()` in `app.py` must cancel five categories of tasks: (1) per-agent I/O tasks via `cancel_background_tasks()` (reader, stderr, resource_flush, wait_and_finalize, ResourceCollector.stop()), (2) WebSocket producer tasks via `_ws_manager.cancel_all()`, (3) batch job tasks via `cancel_all_batches()`, (4) PR throttle/monitor tasks, (5) liveness sweep task. The `cancel_background_tasks()` gather has a 3-second internal timeout; the outer `_shutdown_tasks()` call has a 5-second timeout via `asyncio.wait_for`. WebSocket `_broadcast` uses `asyncio.wait_for(send_json, timeout=2.0)` to prevent blocking on half-closed sockets. `_wait_and_finalize` handles `CancelledError` by calling `process.stop()` before re-raising. Files: `sova/dashboard/app.py`, `sova/dashboard/services/agent_lifecycle.py`, `sova/dashboard/routers/agents.py`, `sova/dashboard/services/batch_service.py`.
- **Reviewer post failure must not trigger address-review**: `ReviewerRole._post_review()` returns `bool` (True=posted, False=all attempts failed) and never raises. `_execute()` sets `review.post_failed = True` on failure. `_write_handoff()` is always called regardless. When `post_failed=True`, the handoff writes `next_action="review_post_failed"` with a single `HandoffAction(id="rerun_review", auto_execute=False)`. `get_sova_review_verdict()` returns `"post_failed"` (not `"revise"`) on that next_action so `_process_auto_handoff()` does not spawn an address-review agent. The default `"revise"` verdict for any missing/null handoff was the root cause of spurious address-review cycles against unreviewed PRs. File: `sova/roles/reviewer.py`, `sova/dashboard/services/agent_recovery.py`. PR #513.
- **SOVA review verdict: label-primary, DB/marker fallback**: the reviewer writes a `sova:approved`, `sova:revise`, or `sova:block` label on the issue after posting findings (`_write_verdict_label()` in `reviewer.py`, non-fatal). `_fetch_sova_verdicts()` in `work_item_service.py` resolves the verdict via a four-level fallback chain: (1) issue `sova:*` label (primary, cross-machine, zero extra API call since queue data already includes issue labels), (2) most recent reviewer `TaskRun`'s `handoff_json.next_action` in the local DB, (3) GitHub PR review body `<!-- sova-review: {verdict} -->` marker, (4) heuristic body-structure detection (`## PR Summary` + `## Verdict`) for pre-marker reviews. Label-derived verdicts use an epoch sentinel for `reviewed_at` so `_is_verdict_stale()` correctly detects human approvals submitted after the SOVA review. The DB path remains authoritative for `reviewed_at` timestamps. Files: `sova/dashboard/services/work_item_service.py`, `sova/roles/reviewer.py`.
- **`_post_review()` must never raise; `_write_handoff()` must always run**: when all three posting attempts fail (formal review API, body-only review API, PR comment fallback), the method returns `False` instead of raising. `ReviewResult.post_failed = True` is set before `_write_handoff()` is called. `_write_handoff()` branches on this flag: when `True`, it writes a `review_post_failed` `AgentHandoff` and a `DashboardHandoff` with a single `rerun_review` action (`auto_execute=False`). `get_sova_review_verdict()` returns `"post_failed"` for `next_action == "review_post_failed"`, which `_apply_sova_verdict()` maps to `PR_AWAITING_REVIEW`. This prevents null `handoff_json` (from an exception propagating before `_write_handoff()` ran) from being misclassified as "revise" by the output-line parser fallback. Files: `sova/roles/reviewer.py`, `sova/dashboard/services/agent_recovery.py`. PR #502.
- **Pipeline variant detection must gate on `current_step`**: `get_step_progress()` and `_detect_variant()` use `role+pr_number` to identify address-review runs, but ONLY when `current_step in (None, "agent")`. The `current_step="agent"` sentinel is set briefly at TaskRun creation and cleared by `_adopt_task_run()` when the WorkflowEngine takes over. WorkflowEngine progresses through real step names and acquires `pr_number` mid-pipeline via `_sync_task_run_context()` after `CreatePRStep` -- without the gate, every developer run that creates a PR would be mislabeled as "address_review" for steps 10-12 (monitor_ci, extract_memory, handoff_to_reviewer). `_detect_variant_from_steps()` is authoritative when step history contains address-review-only steps; otherwise falls back to `get_step_progress()`.
- **Roles must self-discover missing context**: when a role is spawned fresh from the dashboard (not via `--resume`), `ExecutionContext` fields populated by earlier pipeline steps (e.g., `pr_number` from `CreatePRStep`) are `None`. Roles that depend on upstream context must look it up from GitHub rather than failing. Pattern: `ReviewerRole` calls `find_pr_for_issue()` when `ctx.pr_number` is missing.
- **PR deduplication in CreatePRStep**: `CreatePRStep.execute()` calls `find_pr_for_issue()` before `create_pr()`. If an open PR already exists for the issue, it adopts it (`ctx.pr_number = existing.number`) instead of creating a duplicate. `find_pr_for_issue()` verifies PRs by checking for issue-linking keywords (`Closes/Fixes/Resolves #N`) in the body or branch name pattern (`issue-N`), not free-text search, to avoid false positives.
- **State-adopting steps must replicate all side effects**: when a step has an early-return path for "already exists" (e.g., `CreatePRStep` adopting an existing PR), it must still perform all side effects the primary path does -- especially tracker state transitions. Without this, issues get stuck (e.g., remaining `in_progress` when they should be `in_review`). Pattern: audit the primary path for side effects, then replicate them in every skip/adoption path.
- **macOS notifications via terminal-notifier**: `send_desktop_notification()` in `sova/ipc/notifications.py` uses `terminal-notifier` (Homebrew) as the primary path on macOS, supporting custom app icon (`-appIcon`), subtitle, sound, and notification grouping. Falls back to JXA (`osascript -l JavaScript`) if `terminal-notifier` is not installed. JXA fallback uses `json.dumps()` for injection-safe escaping and `soundName: "Glass"`. All call sites pass title="SOVA", subtitle="{Role} {status} #{issue}", message="{project} | {details}", group="sova-{issue}".
- **Dual handoff persistence (file + DB)**: agents write both DB-backed `AgentHandoff` (for orchestrator/scheduler history via `write_handoff()`) and file-based `DashboardHandoff` (for dashboard polling via `write_handoff_file()`). Dashboard reads the file for speed (no async DB in polling loop); scheduler queries the DB for cross-run state. `DashboardHandoff` has `next_actions` (UI buttons); `AgentHandoff` has `pending_findings` (agent context). Both written in every role's `_write_handoff()`.
- **Auto-handoff must clear handoff file before spawning next agent**: `_process_auto_handoff()` in `agent_handoff.py` calls `handoff_service.clear_handoff()` before spawning the next agent via `start_agent()` or `start_command()`. Without this, the newly spawned agent or a concurrent dashboard poll may re-process the same handoff, leading to duplicate agent spawns. The clear must happen synchronously before the spawn call, not after.
- **Stale review-only handoff bypass**: a timing race can leave a developer handoff saying "spawn reviewer" after the reviewer already ran -- if the per-issue handoff file was overwritten by the developer's exit handoff after the reviewer wrote its results. Two guards prevent the stuck state: (1) `compute_work_item_state` in `work_item_service.py`: if all handoff actions are in `_REVIEW_ACTION_IDS = frozenset({"review", "review_pr"})` and `sova_verdict.has_sova_review=True`, the handoff is bypassed and PR state is used directly (so "Integrate PR" is shown instead of staying stuck at "Review PR"). (2) `_process_auto_handoff` in `agent_handoff.py`: before spawning a review-action agent, calls `get_sova_review_verdict` -- if a review already exists, clears the stale handoff and returns without spawning. Manual recovery: `POST /api/handoff/clear {"issue": "N"}`. PR #514.
- **Seed cross-agent data before clearing the handoff file**: when spawning an agent that needs data from the previous agent's handoff (e.g., review findings for address-review), extract the data before `clear_handoff()` and write it to the new TaskRun's `handoff_json` via `start_agent(handoff_findings=...)`. Without this, the address-review step can't find findings when: (1) the handoff file was cleared, (2) `resume_run_id` points to a non-reviewer run, and (3) the issue-level DB fallback fails. File: `sova/dashboard/services/agent_db.py:_seed_handoff_findings()`.
- **Adapter ABC contract**: `TaskAdapter` in `sova/adapters/base.py` defines 19 abstract methods: `list_tasks`, `get_task`, `get_state`, `transition_state`, `assign`, `add_label`, `remove_label`, `_do_post_comment`, `_do_post_pr_comment`, `_do_post_pr_review`, `_do_edit_body`, `link_pr`, `get_pr_reviews`, `get_comments`, `_do_create_issue`, `get_available_transitions`, `list_milestones`, `create_milestone`, `set_milestone`, plus `github_user` field (constructor param). Public posting methods (`post_comment`, `post_pr_comment`, `post_pr_review`, `edit_body`, `create_issue`) are concrete Template Methods that run the egress filter before delegating to `_do_*` implementations. When adding new agent capabilities that interact with the tracker, add the method to the ABC first, then implement in `GitHubAdapter`. Factory: `create_adapter(type, repo, github_user, project_number)`.
- **LLM for user-facing outputs with structured fallback**: workflow steps producing user-facing content (PR descriptions, review comments) use a focused LLM call (Sonnet, ~$0.01) with structured context (commit log, diff stats, issue body). Fallback MUST preserve available data -- build a structured body from already-fetched data rather than discarding to a bare stub. Pattern: `_generate_pr_body()` + `_build_fallback_body()` in `create_pr.py`, `_run_review()` in `reviewer.py`.
- **JIRA-aware pipeline outputs**: when `task_source.is_jira`, the pipeline adapts five output points: (1) `CommitStep` appends `JIRA: https://...` instead of `Closes #N` in commit trailers. (2) `WorktreeStep` produces `feat/PROJKEY-N-slug` branch names instead of `feat/issue-N`. (3) `CreatePRStep` uses a JIRA-aware LLM prompt template (no `Closes #N` for the LLM to generate wrong) and strips branch prefixes when falling back to branch name as PR title. (4) `CreatePRStep` title prepends `[PROJECT_KEY-NUMBER]` for JIRA's GitHub integration. (5) `parse_linked_issue()` in `pr_service.py` recognizes both JIRA markdown links (`[PROJ-42](https://...)`) and plain links (`JIRA: https://.../browse/PROJ-42`). The `/pr` command template (both distributable and SOVA-specific) is also task-source-aware. Files: `commit.py`, `create_worktree.py`, `create_pr.py`, `pr_service.py`, `commands/pr.md`.
- **Rebase with LLM conflict resolution**: `rebase_with_conflict_resolution()` in `sova/git/rebase.py` rebases onto the latest base branch and uses the LLM to resolve merge conflicts. Two-level loop: outer loop iterates over conflicting commits (capped by `max_commits`, default 5, uses `for...else` to detect exhaustion), inner loop retries LLM resolution per commit (capped by `max_attempts`, default 3). This prevents a multi-commit rebase from exhausting all retry attempts on the first commit. Stashes any uncommitted changes before rebasing and pops the stash on every exit path (success, no-conflict failure, conflict abort, loop exhaustion) via a `_pop_stash` closure -- the function can be called on a dirty worktree. If rebase fails with no conflicted files (e.g., uncommitted changes still present, or other git error), the original error is surfaced rather than the misleading "no rebase in progress" message from a failed `--continue`. Aborts on LLM failure or loop exhaustion so the worktree is never left in a broken rebase state. `RebaseStep` in the address-review pipeline runs this before `AddressReviewStep`. `PushStep` uses `--force-with-lease` when `ctx.pr_number` is set (post-rebase history rewrite).
- **`sync_branch` stash guard**: `sync_branch()` in `sova/git/branch.py` stashes uncommitted changes before checkout and restores them after. The `_restore_stash()` helper must be called on every exit path (early returns for worktree conflicts, RuntimeError fallback, checkout failure raise, and the normal path). Uses `git reset --hard origin/{branch}` instead of pull because sync_branch only targets the base branch.
- **Pydantic request models must be at module scope in `app.py`**: `from __future__ import annotations` makes all type annotations lazy strings. FastAPI resolves annotation strings by searching the module's global namespace. Pydantic `BaseModel` subclasses defined inside nested functions (e.g., `_setup_multi_project()`) are not in module globals, so FastAPI falls back to treating the parameter as a query param, producing 422 errors. Always define request/response models at module level. File: `sova/dashboard/app.py`.
- **Thin re-export wrappers during module splits**: when splitting a large module into submodules (e.g., `control_service.py` -> `agent_lifecycle.py` + `agent_output.py` + `agent_recovery.py`), convert the original module to a thin re-export facade (`from agent_lifecycle import X`) rather than deleting it. This preserves backward compatibility for all existing imports in routers, tests, and commands. Delete the old module only after all imports are migrated.
- **Never non-visible overflow on containers with popout children**: the sidebar `<nav>` and its scrollable child div contain absolutely-positioned children (notification panel, CSS hover tooltips) that extend beyond their boundary at `left: 100%`. ANY non-visible overflow value (`hidden`, `auto`, `scroll`) on ANY ancestor creates a clipping boundary for positioned descendants. CSS spec: when one axis is non-visible, the other is computed to `auto` even if explicitly set to `visible` -- so `overflow-y: auto; overflow-x: visible` does NOT work (x becomes `auto` too). Use `max-width` and opacity transitions on individual child elements instead of overflow on the container. This applies to any fixed-position sidebar or panel that contains popout menus or tooltips.
- **Operations persona for oversight**: user-maintained `~/.config/sova/operations_persona.md` defines operational priorities for the oversight agent. No caching: read fresh each cycle so live edits take effect immediately. Single global file (not per-project) ensures consistent oversight behavior fleet-wide. Loaded into `OversightAgent._persona` and exposed via `get_system_prompt()` for LLM context injection by observation/analysis hooks (#445, #446, #447). Dashboard settings page shows a read-only preview with an "Edit" button that launches the OS default editor. Falls back to `DEFAULT_PERSONA_TEMPLATE` on missing/empty/unreadable file. File: `sova/oversight/persona.py`, `sova/oversight/agent.py`.
- **Memory extraction is a no-op**: `ExtractMemoryStep` still runs before every handoff step in both pipelines (step slot preserved for future rule-based extraction), but `extract_memories()` returns immediately without calling the LLM. `ReviewerRole._extract_review_memories()` is also a no-op. Use the human-reviewed `/extract-knowledge` command for knowledge capture. Infrastructure (`_build_extraction_prompt`, `_parse_extraction_response`, `_deduplicate_and_store`) is retained in `sova/knowledge/extraction.py` for future use.
- **Issue Lifecycle Control**: `IssueLifecycle` is the "spine" connecting all `TaskRun` records for a single issue into a unified journey with 6 phases (`LifecyclePhase` enum: development, post_pr, review, address_review, integrate, post_merge). `LifecyclePhaseRecord` tracks each phase execution (status, cost, attempt counter, linked TaskRun). Phase transitions are advisory (warnings, not strict enforcement) -- matches `--force` philosophy. The `post_pr` phase is passive (no TaskRun; inferred from PR creation). `lifecycle_service.py` provides CRUD, phase transitions, and backward-compatible reconstruction from pre-existing TaskRuns via `build_lifecycle_view()`. Integration hooks in `agent_lifecycle.py` (`_link_run_to_lifecycle`, `_finalize_lifecycle_phase`) are non-fatal side effects. Dashboard UI at `/lifecycle/{issue_number}` shows a phase rail with live polling.
- **Doc counts drift after refactors**: step count, service count, adapter method count, CLI subcommand list, and test count go stale after module splits, service extractions, or ABC changes. After any structural refactor, run actual counts (`find`, `grep -c`, `wc -l`) and update AGENTS.md, architecture.md, and CLAUDE.md. The `/review` command checks doc freshness automatically.
- **Stale references persist after file/feature renames**: when a file is deleted, renamed, or a concept changes (e.g., "PAK" -> "SOVA"), references survive in commands, rules, issue bodies, and vision docs. After any rename or deletion, run `grep -rn "OLD_NAME" . --include="*.md"` to find stale references across all markdown files. Also check GitHub Issues and VISION.md for outdated naming.
- **Adapter-type guards must not assume GitHub**: service code that guards with `if not cfg.github_repo: return []` silently blocks Jira-backed projects. When checking whether a task source is configured, check the adapter type first: `if cfg.task_source.type == "github" and not cfg.github_repo`. Or remove the guard and let `create_adapter(cfg)` raise on misconfiguration (it validates required fields per adapter type). File: `sova/dashboard/services/queue_service.py`.
- **CI failure auto-recovery in MonitorCIStep**: when CI checks fail, `MonitorCIStep` enters an internal fix loop (modeled after `ValidateStep`'s pre-push hook fix pattern). The loop: (1) fetches CI failure logs via `get_ci_failure_logs()` (parses run ID from `CICheck.details_url`, calls `gh run view --log-failed`), (2) invokes Claude with the failure context to fix and commit, (3) runs local pre-push hook to validate before pushing, (4) pushes with `--force-with-lease`, (5) re-polls CI. Loops up to `min(ci.max_fix_attempts, 3)` times. Set `ci.max_fix_attempts = 0` in `sova.toml` to disable (pre-existing behaviour). Budget is checked before each attempt. The fix loop is entirely within the step's `execute()` -- no pipeline structure, WorkflowEngine, or step registry changes. File: `sova/core/steps/monitor_ci.py`.
- **Guard against no-op pushes in LLM fix loops**: after invoking the LLM to fix code (CI failures, lint errors), verify a new commit was created before pushing. Use `git rev-list --count origin/{branch}..HEAD` -- if 0, the LLM edited the working tree but didn't commit, or produced no changes at all. Without this guard, the push sends stale HEAD, CI sees the same failure, and the loop wastes an attempt. File: `sova/core/steps/monitor_ci.py:_try_fix_ci()`.
- **Address-review pipeline needs independent worktree discovery**: the address-review pipeline may run on a branch whose worktree was created by a prior developer run. Worktrees can be lost (cleanup, pruning, project rename). `DeveloperRole._discover_address_review_context()` checks `.claude/worktrees/{issue_number}`, then falls back to `find_worktree_by_branch()` when the issue-based directory doesn't exist (guards against returning project_dir as worktree). It also discovers `branch_name` from the PR's `headRefName` via `get_pr_branch()`. All three are defense-in-depth: the reviewer should propagate `branch_name` in its handoff, but the developer self-discovers if it's missing. `start_agent()` in `agent_lifecycle.py` resolves `issue` from `pr_number` via `_resolve_issue_from_pr()` when issue is empty, and resolves the worktree via `_resolve_issue_worktree()` using the PR's branch name -- matching `start_command()`'s pattern. DB operations (`_create_task_run`, `OutputWriter`, lifecycle) always use `project_dir`; only the subprocess spawn uses the worktree `cwd`. Files: `sova/roles/developer.py`, `sova/dashboard/services/agent_lifecycle.py`.
- **Address-review finding loading uses four fallback sources**: `AddressReviewStep` reads findings from: (1) handoff file (`handoff.json`), (2) resumed run's DB handoff (`resume_run_id`), (3) most recent reviewer run by issue number (`_load_review_findings_by_issue()`), (4) GitHub PR review bodies via `_load_findings_from_github_reviews()` (covers reviews posted as body comments that handoff-based loaders miss, using `ctx.adapter.get_pr_reviews()` for the API call). The third fallback is essential when the handoff file is cleared by auto-handoff and `resume_run_id` points to a non-reviewer run. Also fetches CodeRabbit findings from the PR via `_load_coderabbit_findings()` and merges them into the prompt. File: `sova/core/steps/address_review.py`.
- **External review thread lifecycle**: CodeRabbit's `CHANGES_REQUESTED` review state persists across force-pushes -- new commits do NOT auto-dismiss it. After addressing bot findings: (1) reply to each inline comment with the fix or acknowledgement, (2) resolve each conversation thread via `resolveReviewThread` GraphQL mutation, (3) dismiss the `CHANGES_REQUESTED` review via `PUT /reviews/{id}/dismissals` (filter on `user.type == "Bot"` -- never dismiss human reviews). Steps 2 and 3 are separate API operations: dismissing the review does NOT resolve its threads, and resolving threads does NOT dismiss the review. Both must be done or the PR shows either "unresolved conversations" or "changes requested" blockers. The fix-before-reply order is critical: push fixed code first, then reply and resolve -- otherwise CodeRabbit re-reviews unfixed code and re-posts `CHANGES_REQUESTED`. Verify `gh auth status` matches `github_user` in `sova.toml` before thread operations; mismatched accounts get FORBIDDEN. Thread resolution for bot reviewers differs from human reviewers: resolve both Fixed AND Acknowledged bot threads (no human confirmation needed); for human threads only resolve Fixed ones. File: `.claude/commands/address-pr.md` (steps 11-12).
- **address-pr wrong-directory failure mode**: when `_resolve_issue_worktree()` falls back to `project_dir` (e.g., no worktree exists for a manually-developed PR branch), the command agent's CWD is the main repo. If the agent then runs `git checkout <HEAD_BRANCH>`, it switches the main repo to the PR branch. Any uncommitted changes from the run bleed onto `main` when the agent cleans up with `git checkout main`. The `/address-pr` command guards against this: step 3 requires `git branch --show-current` to match `headRefName` before proceeding; if no matching worktree exists, the command stops rather than using `git checkout`. Step 8 (squash/reset) adds a second guard verifying the branch before any `git reset`.
- **Verify `gh auth` account before review thread operations**: `resolveReviewThread` GraphQL mutation and review dismissal API return FORBIDDEN if the active `gh` account lacks write access to the repo. Before replying to or resolving PR threads, verify the active account matches `github_user` in `sova.toml` via `gh auth status`. Switch with `gh auth switch --user <target>` if needed.
- **Headless agent autonomy**: `ClaudeCodeRuntime.spawn()` prepends a `_HEADLESS_PREAMBLE` to every prompt and passes `--permission-mode bypassPermissions`. In Claude CLI's `-p` mode, a text-only `end_turn` (no tool call) causes the process to exit -- so if the model asks "May I proceed?" instead of acting, the agent dies mid-task. The preamble instructs the model to act without confirmation; `bypassPermissions` fully bypasses the permission system (`auto` only auto-approves tools already in the settings.json allowlist, which is insufficient for headless agents that need to edit arbitrary project files). File: `sova/ipc/runtime.py`. Corollary: explicit command step instructions ("Ask the user before posting") override the preamble because they are more specific and closer to the action. Commands that may run headlessly must never contain approval gates, confirmation prompts, or "show draft first" language in any step. Audit commands for such language before shipping. PR #404.
- **Direct subprocess spawn for pipeline roles**: `start_agent()` in `agent_lifecycle.py` uses `spawn_direct()` from `sova/ipc/runtime.py` for pipeline roles (`developer`, `researcher`, `planner`). This spawns `sova run` directly via `asyncio.create_subprocess_exec`, bypassing the Claude Code intermediary. Eliminates the 600s Bash tool timeout that killed long-running develop steps, and saves ~$0.50/run wrapper cost. Non-pipeline roles (`reviewer`, custom) and `start_command()` still go through `ClaudeCodeRuntime.spawn()` since they need Claude's LLM capabilities. `_PIPELINE_ROLES = frozenset({"developer", "researcher", "planner"})` in `runtime.py` controls the split. The output parser (`_parse_stream_line`) handles plain text from `sova run` via JSON fallback; cost tracking comes from the DB (WorkflowEngine), not the stream. Files: `sova/ipc/runtime.py`, `sova/dashboard/services/agent_lifecycle.py`.
- **Two CLI invocation paths must stay in sync**: `ClaudeCodeRuntime.spawn()` (dashboard-spawned non-pipeline agents via `sova/ipc/runtime.py`) and `ClaudeCodeProvider.invoke()` (WorkflowEngine step invocations via `sova/llm/providers/claude_code.py`) both run `claude -p` headlessly but share no argument-building code. When adding flags like `--permission-mode bypassPermissions`, both paths must be updated. `_build_args()` in the provider is the shared helper for `invoke()` and `invoke_streaming()`; the runtime builds its own args inline. On CLI failure, the provider's error handler must check stdout JSON (not just stderr) because `--output-format json` routes error details to stdout. Pipeline roles bypass both paths via `spawn_direct()`.
- **Headless agent prompts must frame CLI commands as bash code blocks**: applies only to non-pipeline roles spawned via `ClaudeCodeRuntime.spawn()`. When `start_agent()` sends `sova run` to Claude Code for reviewer/custom roles, the command is wrapped in bash framing (`Run the following command...` + fenced ```bash block) with `_HEADLESS_PREAMBLE` to prevent the model from interpreting it as a task. Pipeline roles (developer, researcher, planner) bypass this entirely via `spawn_direct()`. Also use `shlex.quote()` on string tokens for defense-in-depth. Files: `sova/ipc/runtime.py`, `sova/dashboard/services/agent_lifecycle.py`.
- **Dashboard JS polling must clear stale UI on negative path**: when JS polls an API for transient state (running agents, pending handoffs, batch progress), the negative path (state gone) must actively clear the UI element's `innerHTML` or add `hidden`, not just `return`. If state changes between polls (agent finishes, handoff is dismissed), orphaned panels persist until page reload. Pattern: iterate all containers at the top of the poll handler and clear when the state is absent. Applies to: `loadAgents()` (agents-grid), `checkStandaloneHandoff()` (handoff-panel), `_pollGlobalBatch()` (batch progress bar). Corollary: when JS tracks interactive state flags (e.g., `_cardMenuOpen`) to skip polling re-renders while a dropdown is open, ANY function that replaces the grid via `innerHTML` must also reset the flag -- otherwise subsequent polls skip rendering indefinitely until a user click event resets it. This includes `renderTaskCards()` (called from search, expand, collapse), not just the poll handler.
- **New config sections need triple registration**: adding a Pydantic model to `ProjectConfig` requires: (1) model class in `sova/config/models.py`, (2) section name in `_NESTED_SECTIONS` in `sova/config/loader.py` (without this, `[section]` in sova.toml is silently ignored), (3) metadata in `sova/dashboard/settings_meta.py` for UI display. Caveat: `_NESTED_SECTIONS` passes TOML values as init kwargs, which outrank `SOVA_*` env vars in Pydantic Settings.
- **Per-issue handoff files for parallel agent isolation**: agents write to `handoff-{issue}.json` instead of a shared `handoff.json`. This eliminates race conditions when multiple agents finish concurrently. `write_handoff_file` uses `handoff.issue` for the filename. `read_handoff_file(issue=N)` reads a specific file; without issue, returns the most recent. `read_all_handoff_files` returns all active handoffs. All readers (`_process_auto_handoff`, `_load_review_findings`, `_read_file_handoff`) pass issue context. The `GET /handoff` API returns `{handoff, handoffs[]}` for backward compatibility. Legacy `handoff.json` is supported as a fallback. Files: `sova/ipc/handoff.py`, `sova/dashboard/services/handoff_service.py`.
- **`recover_stale_runs` must check external state for merge-role runs**: when `recover_stale_runs()` finds a dead-PID run with role in `{integrate-pr, approve-merge}` and a `pr_number`, check `_check_pr_merged_on_failure` before marking interrupted. If the PR is merged, mark as "done" instead. Prevents false "interrupted" banners on dashboard restart after the agent completed its work but the process died during cleanup. File: `sova/dashboard/services/agent_recovery.py`.
- **Config-driven provider selection must be wired at startup**: adding a config field (`llm.provider`) + factory (`create_provider()`) + ABC without calling `set_provider(create_provider(cfg.llm.provider))` at app startup means the config has no effect. Wire init in `_init_llm_provider()` (CLI) and `create_app()` (dashboard). For multi-project mode, gate behind `if not is_multi:` or resolve per-request. Files: `sova/llm/provider.py`, `sova/cli/app.py`, `sova/dashboard/app.py`.
- **`pull_request_target` reads workflow from base branch, not PR branch**: GitHub's `pull_request_target` event always uses the workflow YAML from the base branch (e.g., `main`), even for fork PRs. This makes it tamper-proof -- fork authors cannot modify the workflow. Use it for security-sensitive automation like auto-approving CI runs, posting status comments, or running SonarCloud scans with secrets. The `GITHUB_TOKEN` in `pull_request_target` context has write permissions (unlike `pull_request` for forks, which is read-only). Caveat: CI workflow changes in a PR don't take effect for that PR's own `pull_request_target` run -- they only apply after merge. Fork-owner conditions (`github.event.pull_request.author_association`) should be used to scope actions to external contributors. Files: `.github/workflows/fork-pr-gate.yml`, `.github/workflows/sonarcloud.yml`.

- **Pipeline outcome validation detects bypass at exit time**: `_validate_pipeline_outcome()` in `agent_db.py` checks whether a pipeline role (developer, researcher, planner) actually executed WorkflowEngine steps. Primary signal: `current_step == "agent"` (sentinel never cleared) AND 0 `StepExecution` records. Secondary signal (developer-only): `create_pr`/`push` step completed but `pr_number` is None. Tertiary signal (developer-only): `commit` step completed but neither `push` nor `create_pr` ran -- pipeline exited mid-flight after committing code. Quaternary signal (all pipeline roles): last `StepExecution` has `status=interrupted` (set by `_finalize_orphaned_steps` when the process exits with a step still running) -- the pipeline crashed or exited silently mid-step. Called from `_wait_and_finalize()` after `_validate_command_outcome()` (they are mutually exclusive: command roles vs pipeline roles). Downgrades "done" to "failed" via `_downgrade_to_failed()`. `_PIPELINE_ROLES = {"developer", "researcher", "planner"}` excludes `reviewer` (it's in `_STANDALONE_ROLES` and doesn't use WorkflowEngine). Files: `sova/dashboard/services/agent_db.py`, `sova/dashboard/services/agent_lifecycle.py`. Related issues: #343 (auto-retry), #344 (runtime watchdog), #628 (orphaned steps).
- **Orphaned step finalization on terminal transition**: `_finalize_orphaned_steps()` in `agent_db.py` runs inside `_finalize_task_run()` after setting the terminal status. Updates any `StepExecution` records still in `status=running` to `status=interrupted` with `ended_at=now()`. Without this, the dashboard step progress bar shows "running" for a step in a terminal run. Called within the same DB transaction as the TaskRun status update for consistency.
- **Auto-retry for recoverable pipeline failures**: `_schedule_retry()` in `agent_lifecycle.py` is called from `_wait_and_finalize()` when a pipeline role exits with non-zero status. Flow: (1) `classify_failure(error_message, exit_code)` pattern-matches against `_RECOVERABLE_PATTERNS` / `_TERMINAL_PATTERNS` tuples in `agent_db.py` -- terminal-first scan order, RECOVERABLE default for unknown errors; (2) `_get_retry_state()` fetches `retry_count` from the failed run and checks for existing nonterminal runs in the same session; (3) after `retry_delay_seconds` sleep, `_has_nonterminal_run_for_issue()` re-checks for concurrent runs spawned during the delay window; (4) `start_agent(force=True)` spawns the retry with `retry_of_id` and `retry_count+1` threaded into the new `TaskRun`. Both config knobs (`pipeline.max_retries`, `pipeline.retry_delay_seconds`) are triple-registered (model, loader, settings UI). Terminal failures (budget exceeded, auth errors, rejection) never enter retry logic. The `FailureCategory` enum and `classify_failure()` live in `agent_db.py`; retry orchestration lives in `agent_lifecycle.py`. HTTP status code patterns (502/503/504) use "http " prefix to avoid false positives on file paths or port numbers containing those digits. The 429 pattern uses " 429" (space prefix) for the same reason. Terminal "rejected" uses "task rejected"/"spec rejected" to avoid false positives on generic "rejected" substrings in unrelated messages.

- **GitHub API calls must not run inside DB write transactions in sweep/recovery paths**: `_liveness_sweep_once` and `recover_stale_runs` use a three-phase pattern to avoid write lock contention: (1) read-only query with no write lock, (2) I/O checks (GitHub API) outside any session with bounded timeout, (3) write-only transaction for status updates. In the write phase, guard with `if run.status in _TERMINAL: continue` -- `_wait_and_finalize()` may have finalized the run between phases 1 and 3. For `recover_stale_runs`, use `asyncio.gather` for concurrent checks wrapped in outer `asyncio.wait_for(timeout=20)` so N stale merge-role runs don't multiply startup time. Files: `sova/dashboard/app.py:_liveness_sweep_once`, `sova/dashboard/services/agent_recovery.py`. PR #364.

- **`_resolve_issue_worktree` creates worktrees proactively**: when a branch is known but no worktree directory exists (common for issue-less PRs), calls `create_worktree()` -- naming: `pr-{N}` for issue-less PRs, `{issue_id}` for issue-linked PRs, sanitized branch name otherwise. Falls back to `project_dir` only on git failure. Both `start_agent` and `start_command` thread `pr_number` through to this function. Prevents agents from running in the main repo and triggering uvicorn `--reload` via watched file changes in `sova/dashboard/`. File: `sova/dashboard/services/agent_context.py:_resolve_issue_worktree`. PR #364.

- **Subprocess agents must resolve project root from linked worktree CWD**: when the dashboard spawns an agent into `.claude/worktrees/<id>`, `Path.cwd()` inside the subprocess is the worktree, not the project root. `get_session()`, `load_config()`, and `_adopt_task_run()` all use this path, so they write to the worktree DB and fail to adopt the dashboard's TaskRun. `sova run` uses `get_primary_worktree_root()` (`git rev-parse --git-common-dir`: absolute path = linked worktree, relative `.git` = primary) to resolve the real project root before any DB/config calls. `write_handoff()` in the subprocess still needs `project_dir` passed explicitly (it calls `get_session()` without project_dir); the dashboard backstops this via `_persist_completing_agent_handoff()` in `_process_auto_handoff()`. Files: `sova/git/worktree.py:get_primary_worktree_root`, `sova/cli/commands/run.py`, `sova/dashboard/services/agent_handoff.py`. PR #373.

- **`start_agent()` recovers pr_number from DB history for developer role**: when `recover_stale_runs()` marks an in-progress developer run as "interrupted", the `--pr` context is lost. The next `start_agent(issue=N, role="developer")` without an explicit `pr_number` would fail at `AssessStep` (open PR detected). `_recover_last_pr_number()` queries the most recent `{"interrupted","failed","paused"}` developer `TaskRun` (NOT "done" -- done runs may have merged PRs) with non-null `pr_number` and forwards it. Called outside `pa._lock` (read-only). File: `sova/dashboard/services/agent_lifecycle.py:_recover_last_pr_number`. PR #373.

- **`PR_CHANGES_REQUESTED` split into `PR_SOVA_CHANGES` and `PR_EXTERNAL_CHANGES`**: the unified state conflated two different response paths. `PR_SOVA_CHANGES` is returned when `get_sova_review_verdict()` says revise/block -- routes to developer agent (address_review pipeline with structured handoff). `PR_EXTERNAL_CHANGES` is returned when GitHub `review_decision == "CHANGES_REQUESTED"` from CodeRabbit or a human reviewer with no SOVA override -- routes to `/address-pr` command (thread resolution). `PR_EXTERNAL_CHANGES` is in `_VERDICT_OVERRIDEABLE`: SOVA verdict always wins. `PR_CHANGES_REQUESTED` is kept for backward compatibility. Config-aware: `external_reviews.enabled = false` (default) means CI-passed PRs with no SOVA review show `PR_AWAITING_REVIEW` ("Review" primary) instead of `PR_SOVA_PENDING` ("Address PR" primary) -- skips the inline-comment step for bot-free projects. File: `sova/dashboard/services/work_item_service.py`. PR #378.

- **Dual-evaluation PR state experiment**: `ActionFeedback` DB table stores LLM suggestions alongside deterministic action choices plus user ground-truth clicks. Architecture: (1) `llm_suggestion_service.py` calls Anthropic Haiku API directly via httpx, returns `{action_id, action_label, reasoning, disagrees}`, caches 5 min per `(pr_number|state|computed_state)`; (2) `POST /api/prs/{pr_number}/suggestion` serves suggestions lazily (204 when no API key); (3) `POST /api/prs/feedback` writes `ActionFeedback` records; (4) work items include `sova_context: {has_sova_review, verdict}` for the JS to pass to the suggestion endpoint; (5) JS fires suggestions after card render, injects inline widget only when `disagrees=True`, clears stale suggestions on state change. The accumulated `ActionFeedback` rows are the ground truth for improving the deterministic model. Files: `sova/dashboard/services/llm_suggestion_service.py`, `sova/dashboard/routers/prs.py`, `sova/db/models.py:ActionFeedback`, `sova/dashboard/templates/agents.html`. Migration: 021. PR #399.

## Cross-References (domain-specific details)

The following topics have detailed implementation guidance in dedicated guideline docs. Architecture.md covers the architectural decisions; the guideline docs cover patterns, code examples, and gotchas.

- **Migration system** (create_all vs Alembic, alembic_version cases, engine disposal, self-healing fallback): see `docs/database-guidelines.md`
- **Session management** (context manager pattern, expire_on_commit, JSON column NULL): see `docs/database-guidelines.md`
- **Exception hierarchy** (SonarCloud S5713, parent/child catch tuples): see `docs/error-handling-guidelines.md`
- **Non-fatal side effects** (try/except pattern, exc_info=True): see `docs/error-handling-guidelines.md`
- **Timeout hierarchy** (step timeout, CI polling, shell commands, budget limits): see `docs/performance-guidelines.md`
- **GitHub CLI integration** (GH_TOKEN override, gh auth switch, gh pr create URL, PR reviews vs comments): see `docs/integration-guidelines.md` and `docs/api-contracts-guidelines.md`
- **Subprocess safety** (suspicious file guard, shlex.quote): see `docs/security-guidelines.md`
- **Test isolation patterns** (file-backed services, patch.object facades, mock patterns): see `docs/testing-guidelines.md`
